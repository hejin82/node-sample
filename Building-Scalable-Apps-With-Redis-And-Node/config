var config = {
    port: 3001,
    secret: "secret",
    redisPort: 6379,
    redisUrl: "redis://localhost",
    routes: {
        login: "/login",
        logout: "/logout"
    }
};

module.exports = config;
